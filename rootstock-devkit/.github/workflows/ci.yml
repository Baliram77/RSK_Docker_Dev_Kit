name: ci

on:
  push:
  pull_request:

jobs:
  devkit:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}

    steps:
      # Pin to commit SHAs (supply-chain hygiene). Tag annotations: v4.2.2, v1
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

      - name: Install Foundry
        uses: foundry-rs/foundry-toolchain@8789b3e21e6c11b2697f5eb56eddae542f746c10
        with:
          version: nightly

      # NOTE: GitHub-hosted macOS runners don't provide a Docker daemon, so we can’t
      # run docker compose there. We still validate Foundry on macOS.
      - name: Start devkit (docker compose, full profile)
        if: runner.os == 'Linux'
        working-directory: rootstock-devkit
        run: docker compose --profile full up -d

      - name: Wait for RSKj RPC
        if: runner.os == 'Linux'
        run: |
          for i in $(seq 1 60); do
            if curl -fsS -H 'Content-Type: application/json' \
              -d '{"jsonrpc":"2.0","method":"web3_clientVersion","params":[],"id":1}' \
              http://127.0.0.1:4444/ >/dev/null; then
              echo "RSKj is healthy"
              exit 0
            fi
            sleep 2
          done
          echo "RSKj did not become healthy in time"
          docker compose -f rootstock-devkit/docker-compose.yml ps
          docker logs --tail=200 rootstock-rskj-regtest || true
          exit 1

      - name: Blockscout API check (200, with retries)
        if: runner.os == 'Linux'
        run: |
          for i in $(seq 1 60); do
            if curl -fsS -o /dev/null http://127.0.0.1:4001/api/v2/blocks; then
              echo "Blockscout API ready"
              exit 0
            fi
            sleep 2
          done
          echo "Blockscout API did not return 200 in time"
          docker compose -f rootstock-devkit/docker-compose.yml --profile full ps || true
          docker logs --tail=200 rootstock-blockscout-backend || true
          exit 1

      - name: Forge build + test
        working-directory: rootstock-devkit/foundry
        run: |
          forge build
          forge test

      - name: Shutdown
        if: always()
        working-directory: rootstock-devkit
        run: |
          if [ "${{ runner.os }}" = "Linux" ]; then
            docker compose --profile full down -v --remove-orphans
          fi
        shell: bash
